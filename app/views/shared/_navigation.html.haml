%nav.navbar.navbar-default.navbar-fixed-top.light-blue-gradient{ role: "navigation" }
  .container
    .navbar-header
      %button.navbar-toggle{ "type" => "button", "data-toggle" => "collapse", "data-target" => "#navbar_links" }
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to("Misioneros de Los Cabos", root_path, class: "navbar-brand")
    #navbar_links.navbar-collapse.collapse
      %ul.nav.navbar-nav
        %li.photos_tab
          = link_to(t("navigation.photos"), photos_path)
        - if authorized_user_signed_in?
          %li.notices_tab
            = link_to(t("navigation.notices"), notices_path)
        - if authorized_user_signed_in?
          %li.uploads_tab
            = link_to(t("navigation.uploads"), documents_path)
        - if authorized_user_signed_in?
          %li.maintenance_fee_tab
            = link_to(t("navigation.maintenance_fee"), maintenance_fee_path)
        %li.committees_tab
          = link_to(t("navigation.committees"), committees_path)
        - if admin_signed_in?
          %li.admin_tab
            = link_to(t("navigation.admin"), manage_users_path)
      .navbar-right.language_select
        = link_to(image_tag("en_flag.jpg", class: "language_flag", alt: "English"), locale: :en)
        = link_to(image_tag("es_flag.jpg", class: "language_flag", alt: "Español"), locale: :es)
      = render "shared/login"
        